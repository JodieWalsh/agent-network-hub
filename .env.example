# Supabase Configuration
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-key"
VITE_SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_ACCESS_TOKEN="your-access-token"

# Mapbox API (for location autocomplete and geocoding)
# Sign up for free at https://account.mapbox.com/auth/signup/
# Free tier: 100,000 requests/month
VITE_MAPBOX_ACCESS_TOKEN="your-mapbox-token"

# Stripe Configuration (Payment Processing)
# Sign up at https://dashboard.stripe.com/register
# Get API keys from https://dashboard.stripe.com/test/apikeys
#
# IMPORTANT: Use TEST keys during development (pk_test_xxx, sk_test_xxx)
# Switch to LIVE keys only in production (pk_live_xxx, sk_live_xxx)
#
# Publishable key - safe to expose in frontend code
VITE_STRIPE_PUBLISHABLE_KEY="pk_test_xxx"
#
# Secret key - NEVER expose in frontend code, only use in Edge Functions
STRIPE_SECRET_KEY="sk_test_xxx"
#
# Webhook secret - for verifying webhook signatures
# Get from https://dashboard.stripe.com/test/webhooks or Stripe CLI
STRIPE_WEBHOOK_SECRET="whsec_xxx"
